# Server Configuration
PORT=5050
NODE_ENV=production # Use 'production' for prod deployment, 'development' for local

# MongoDB Connection
# For production database
MONGODB_URI=mongodb+srv://username:password@hannahs-habits.gqtcz.mongodb.net/hannahs-habits-prod?retryWrites=true&w=majority&appName=hannahs-habits

# For development database (comment/uncomment as needed)
# MONGODB_URI=mongodb+srv://username:password@hannahs-habits.gqtcz.mongodb.net/hannahs-habits-dev?retryWrites=true&w=majority&appName=hannahs-habits

# JWT Secret (generate a strong unique value for production)
JWT_SECRET=your_jwt_secret_key

# Frontend URL - CRITICAL for OAuth redirects
# For local development
FRONTEND_URL=http://localhost:3000
# For production
# FRONTEND_URL=https://habits.rubygal.com

# Google Photos API Credentials
# Create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Google OAuth Redirect URI - CRITICAL CONFIGURATION
# This MUST match exactly what's configured in Google Cloud Console OAuth screen
# For local development:
GOOGLE_REDIRECT_URI=http://localhost:5050/api/photos/auth-callback
# For production:
# GOOGLE_REDIRECT_URI=https://habits.rubygal.com/api/photos/auth-callback

# IMPORTANT: In Google Cloud Console, you need to configure TWO redirect URIs:
# 1. API callback URI (matches GOOGLE_REDIRECT_URI above)
# 2. Frontend callback URI: http://localhost:3000/photos/callback (for local)
#    or https://habits.rubygal.com/photos/callback (for production)